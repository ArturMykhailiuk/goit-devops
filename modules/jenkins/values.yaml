persistence:
  enabled: true
  storageClass: gp2
  size: 10Gi

controller:
  image:
    tag: "2.516.2"
  overwritePlugins: true
  installPlugins:
    - configuration-as-code:1985.vdda_32d0c4ea_b_
    - kubernetes:latest
    - workflow-aggregator:latest
    - git:latest
    - credentials-binding:latest
    - github:latest
    - docker-plugin:latest
    - docker-workflow:latest
    - job-dsl:latest
  admin:
    username: admin
  # password буде передано з Terraform через set_sensitive
  # password: admin
  serviceType: LoadBalancer
  resources:
    requests:
      cpu: "500m"
      memory: "1Gi"
    limits:
      cpu: "1"
      memory: "2Gi"
  JCasC:
    configScripts:
      credentials: |-
        credentials:
          system:
            domainCredentials:
              - credentials:
                  - usernamePassword:
                      scope: GLOBAL
                      id: github-token
                      username: ArturMykhailiuk
                      password: CHANGEME
                      description: GitHub PAT
      jobs: |-
        jobs:
          - script: >
              job('seed-job') {
                description('Job to generate pipeline for Django project')
                scm {
                  git {
                    remote {
                      url('https://github.com/ArturMykhailiuk/goit-devops.git')
                      credentials('github-token')
                    }
                    branches('*/main')
                  }
                }
                steps {
                  dsl {
                    text('''
                      pipelineJob("goit-django-docker") {
                        definition {
                          cpsScm {
                            scm {
                              git {
                                remote {
                                  url("https://github.com/ArturMykhailiuk/goit-devops.git")
                                  credentials("github-token")
                                }
                                branches("*/main")
                              }
                            }
                          }
                        }
                      }
                    ''')
                  }
                }
              }
